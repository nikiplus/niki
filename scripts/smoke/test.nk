// M0 smoke: minimal language subset (arithmetic, control flow, return).
func main() -> int {
    var a: int = 10;
    var b: int = 20;
    var c: int = a + b;
    if (c > 25) {
        return c;
    }
    return 0;
}
